#set(COMPONENT_SRCS "ota.c")
#set(COMPONENT_ADD_INCLUDEDIRS ".")

# Embed the server root certificate into the final binary
#set(COMPONENT_EMBED_TXTFILES "/home/boss/code/esp/wifi-example/server_certs/ca_cert.pem")

#register_component()
